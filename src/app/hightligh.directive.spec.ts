import { HightlighDirective } from './hightligh.directive';

describe('HightlighDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlighDirective();
    expect(directive).toBeTruthy();
  });
});
